name: Build

on:
  pull_request:
    branches:
      - develop

permissions:
  contents: write

concurrency:
  group: build-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  format-resource-refresh-test:
    name: Format resource refresh test once
    runs-on: ubuntu-latest

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install package dependencies
        run: flutter pub get

      - name: Format resource refresh test
        run: dart format test/application/resource_controller_test.dart

      - name: Discard dependency-tool side effects
        shell: bash
        run: |
          git restore analysis_options.yaml 2>/dev/null || true
          git restore example/analysis_options.yaml 2>/dev/null || true
          git restore example/pubspec.lock 2>/dev/null || true

      - name: Guard scope
        shell: bash
        run: |
          changed="$(git diff --name-only)"
          test "$changed" = "test/application/resource_controller_test.dart"

      - name: Commit formatting
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add test/application/resource_controller_test.dart
          git commit -m "style: format resource refresh test"
          git push

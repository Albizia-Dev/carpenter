# Carpenter development example

This Flutter application verifies Carpenter as an external local dependency.

Run it from this directory with:

```sh
flutter run
```

The public Carpenter UI API is intentionally empty during S0. Future vertical
slices will add real reference integrations here.
